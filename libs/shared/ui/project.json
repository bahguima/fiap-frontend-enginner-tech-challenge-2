{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "shared-ui",
  "sourceRoot": "libs/shared/ui/src",
  "projectType": "library",
  "tags": ["type:shared", "scope:shared", "layer:ui"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/shared/ui --max-warnings=0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/shared/ui/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config jest.config.mjs libs/shared/ui --runInBand"
      }
    }
  }
}
